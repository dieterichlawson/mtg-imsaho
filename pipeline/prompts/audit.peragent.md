## Card to audit: {card}

### Implementation file
`mtg-engine/src/cards/isd/{card_snake}.rs`

### Oracle text (pre-fetched from Scryfall)

{oracle}

### Output
Write your audit as JSON to `pipeline/staging/{run_id}.json` matching the
schema in the shared prompt.

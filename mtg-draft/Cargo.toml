[package]
name = "mtg-draft"
version = "0.1.0"
edition = "2021"

[dependencies]
mtg-engine = { path = "../mtg-engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"

[lints]
workspace = true

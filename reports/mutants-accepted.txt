# Accepted surviving mutants: judged equivalent or defensive-only, with
# reasons recorded in reports/mutation-testing.md. The weekly-mutants
# workflow strips line:col before comparing, so entries here omit them.
mtg-engine/src/stack.rs: delete match arm TargetRequirement::GraveyardCard in is_target_legal
mtg-engine/src/stack.rs: delete match arm TargetRequirement::GraveyardCreatureOfSubtype(subtype) in is_target_legal
mtg-engine/src/stack.rs: delete match arm TargetRequirement::GraveyardCardOwnedByCaster in is_target_legal
mtg-engine/src/stack.rs: delete match arm TargetRequirement::GraveyardCardOwnedByOpponent in is_target_legal
mtg-engine/src/stack.rs: delete match arm TargetRequirement::GraveyardCardOwnedByTargetPlayer in is_target_legal
mtg-engine/src/stack.rs: replace && with || in is_target_legal
mtg-engine/src/stack.rs: replace != with == in is_target_legal
mtg-engine/src/triggers/collect/combat.rs: replace match guard o.zone == Zone::Battlefield with true in attackers_declared
mtg-engine/src/triggers/collect/combat.rs: replace match guard o.zone == Zone::Battlefield with true in blockers_declared
mtg-engine/src/combat.rs: replace > with >= in declare_blockers_with_registry
mtg-engine/src/combat.rs: replace || with && in deal_damage_step
mtg-engine/src/destruction.rs: replace || with && in death_event
# `change_life` returns early on delta == 0, so by the verb test the delta is
# non-zero and `> 0` and `>= 0` name the same set. Equivalent, not untested:
# the `== 0` mutation of the same comparison IS killed, by
# `a_life_gain_is_logged_as_a_gain_with_the_resulting_total`.
mtg-engine/src/state.rs: replace > with >= in GameState::change_life

# RNG internals: the engine's contract is determinism (pinned by the replay
# spot check in scripts/fuzz.sh), not any particular mixing function or
# which side of a coin is "heads".
mtg-engine/src/state.rs: replace ^ with & in GameState::next_random
mtg-engine/src/state.rs: replace ^ with | in GameState::next_random
mtg-engine/src/state.rs: replace >> with << in GameState::next_random
mtg-engine/src/state.rs: replace default_rng_seed -> u64 with 0
mtg-engine/src/state.rs: replace default_rng_seed -> u64 with 1
mtg-engine/src/state.rs: replace == with != in GameState::flip_coin
# Runner-facing helper: no engine path calls it; mtg-runner's game-end
# handling is its consumer.
mtg-engine/src/state.rs: delete ! in GameState::alive_player_count
mtg-engine/src/state.rs: replace GameState::alive_player_count -> usize with 0
mtg-engine/src/state.rs: replace GameState::alive_player_count -> usize with 1
# TemporaryEffect::ModifyPTWhileSourceInPlay: engine vocabulary nothing in
# the pool emits yet. (Normalization caveat: these lines would also mask a
# regression in the plain ModifyPT arm, which is separately pinned by
# continuous_effects.rs::a_targeted_pump_changes_only_its_target...)
mtg-engine/src/state.rs: replace match guard *target == id with true in GameState::effective_power
mtg-engine/src/state.rs: replace match guard *target == id with false in GameState::effective_toughness
mtg-engine/src/state.rs: replace match guard *target == id with true in GameState::effective_toughness
mtg-engine/src/state.rs: replace == with != in GameState::effective_toughness
mtg-engine/src/state.rs: replace += with *= in GameState::effective_toughness
mtg-engine/src/state.rs: replace += with -= in GameState::effective_toughness
# Display/log text only — no rules outcome depends on these.
mtg-engine/src/cards/mod.rs: delete match arm CardType::Artifact in <impl std::fmt::Display for TargetFilter>::fmt
mtg-engine/src/cards/mod.rs: delete match arm CardType::Enchantment in <impl std::fmt::Display for TargetFilter>::fmt
mtg-engine/src/actions.rs: replace && with || in <impl std::fmt::Display for Action>::fmt
# Identity mutant: the default body already is the empty vector.
mtg-engine/src/cards/mod.rs: replace CardBehavior::grants_player_protection_from -> Vec<crate::types::Color> with vec![]
mtg-engine/src/triggers.rs: replace TriggerEvent::phrase -> &'static str with ""
mtg-engine/src/triggers.rs: replace TriggerEvent::phrase -> &'static str with "xyzzy"
# get_id_by_name's <= flip re-looks-up the same failed key: identical result.
mtg-engine/src/cards/mod.rs: replace < with <= in CardRegistry::get_id_by_name
# Per-card files outside the engine core (src/cards/*.rs legacy cards):
# audited and acceptance-tested card by card, same scope rule as src/cards/isd/.
mtg-engine/src/cards/coral_merfolk.rs: delete field subtypes from struct CardData expression in <impl CardBehavior for CoralMerfolk>::card_data
mtg-engine/src/cards/doom_blade.rs: replace && with || in <impl CardBehavior for DoomBlade>::is_valid_target
mtg-engine/src/cards/goblin_piker.rs: delete field subtypes from struct CardData expression in <impl CardBehavior for GoblinPiker>::card_data
mtg-engine/src/cards/grizzly_bears.rs: delete field subtypes from struct CardData expression in <impl CardBehavior for GrizzlyBears>::card_data
mtg-engine/src/cards/holy_strength.rs: delete field subtypes from struct CardData expression in <impl CardBehavior for HolyStrength>::card_data
mtg-engine/src/cards/kalonian_tusker.rs: delete field subtypes from struct CardData expression in <impl CardBehavior for KalonianTusker>::card_data
mtg-engine/src/cards/pacifism.rs: delete field subtypes from struct CardData expression in <impl CardBehavior for Pacifism>::card_data
mtg-engine/src/cards/savannah_lions.rs: delete field subtypes from struct CardData expression in <impl CardBehavior for SavannahLions>::card_data
mtg-engine/src/cards/swords_to_plowshares.rs: replace > with >= in <impl CardBehavior for SwordsToPlowshares>::on_resolve
# Identity mutant: both call sites only add the pair to P/T, so Some((0,0))
# and the default None are indistinguishable.
mtg-engine/src/cards/mod.rs: replace CardBehavior::self_static_pt_mod -> Option<(i32, i32)> with Some((0, 0))
# A zero-cost loyalty ability makes both halves false either way: -0 is 0,
# and 0 is never greater than a u32 loyalty count.
mtg-engine/src/engine/legal/abilities.rs: replace < with <= in loyalty

# The auto-pass gate: whether a seat with nothing meaningful to do is asked
# at all. A wrong answer shows one extra prompt or skips one, and the game
# is unaffected either way — `advance_or_resolve` runs regardless. Its own
# comment says it is deliberately conservative. Preference, not a rule
# (docs/mutation-testing-guide.md).
mtg-engine/src/engine/cards_flow.rs: delete ! in has_castable_with_potential_mana
mtg-engine/src/engine/cards_flow.rs: replace && with || in has_castable_with_potential_mana
mtg-engine/src/engine/cards_flow.rs: replace == with != in has_castable_with_potential_mana
mtg-engine/src/engine/cards_flow.rs: replace || with && in has_castable_with_potential_mana

# The auto-pass livelock guard (`auto_pass_count > MAX_AUTO_PASSES`). A safety
# net, not a rule: disabling it or moving it by one turns a hung game into a
# hung game, which is what the nightly fuzzer's runner reports. Scoped to the
# counter — the `>=` in `consecutive_passes >= num_players` is CR 117.4 and is
# NOT accepted.
mtg-engine/src/engine.rs: replace += with *= in run_game_loop_inner
mtg-engine/src/engine.rs: replace > with == in run_game_loop_inner
mtg-engine/src/engine.rs: replace > with >= in run_game_loop_inner

# `compute_autotap`'s internal simulation. The planner chooses among plans
# that are all correct — colour preservation, utility lands, filter ordering
# — and has been retuned twice (#114, #252). Its contract is property-tested
# instead: an offered plan pays the cost, and a `None` means no plan would
# have (mana.rs `every_tap_plan_the_planner_offers_pays_the_cost_it_was_asked_for`
# and `the_planner_declines_only_when_no_tap_plan_would_have_worked`). Pinning
# the arithmetic would freeze the heuristic; the property catches the same
# class and more. The worked example in docs/mutation-testing-guide.md.
mtg-engine/src/mana.rs: replace += with *= in compute_autotap
mtg-engine/src/mana.rs: replace += with -= in compute_autotap
mtg-engine/src/mana.rs: replace - with + in compute_autotap
mtg-engine/src/mana.rs: replace - with / in compute_autotap
mtg-engine/src/mana.rs: replace -= with += in compute_autotap
mtg-engine/src/mana.rs: replace -= with /= in compute_autotap
mtg-engine/src/mana.rs: replace > with == in compute_autotap
mtg-engine/src/mana.rs: replace > with >= in compute_autotap

# No card in the pool has an EndCombat triggered ability, so the two arms that
# map that step to that trigger kind are unreachable. (The `if *step ==
# Step::EndCombat` block above them, which flushes the delayed token-exile
# queue, IS reachable and is not accepted.)
mtg-engine/src/triggers/collect/timing.rs: delete match arm crate::cards::TriggerKind::EndCombat in step_started
mtg-engine/src/triggers/collect/timing.rs: delete match arm crate::types::Step::EndCombat in step_started

# `was_on_the_battlefield(..) && is_creature(..)` guarding a combat-damage
# trigger. Both operands are always true where this runs: only creatures deal
# combat damage (CR 510.1), and a source that dealt it and then died has its
# `CreatureDied` in the same event window, so `was_on_the_battlefield` finds
# it. Deleting either half survives too — verified by hand. The conjunction is
# defensive, and the half that carries the rule (CR 603.10, an ability
# triggers though its source has left) is pinned by
# `a_combat_damage_trigger_fires_from_a_source_that_died_dealing_it`.
mtg-engine/src/triggers/collect/damage.rs: replace && with || in combat_damage

# `entered_battlefield`'s watcher pre-filter, `(zone == Battlefield || zone ==
# Graveyard)`. `CardBehavior::trigger_zones` is the authoritative gate a few
# lines below, and it defaults to `[Battlefield]` with no card overriding it —
# so the graveyard half admits candidates the real gate then rejects, and
# flipping it changes nothing. Masks two sites in the same function that ARE
# killed: the battlefield half by
# `the_spirit_a_death_trigger_leaves_is_a_creature_entering` and the trigger-kind
# comparison by `undead_alchemist_mills_instead_of_damage`. Becomes productive
# again the day a card watches creatures entering from a graveyard.
mtg-engine/src/triggers/collect/zones.rs: replace == with != in entered_battlefield

# `run_entering_pass`'s second pass restates `copy_of: current.copy_of` over
# `..next`. Equivalent in this pool: `next` is what the card's own
# `replace_event` returned when handed `current`, and every one of them builds
# its answer with `..e.clone()`, so `next.copy_of == current.copy_of` already.
# The restatement is what enforces CR 616.1's ordering (copy effects, then
# everything else) against a card that does not. Its sibling — the FIRST
# pass's `copy_of: next.copy_of` — is not accepted and is killed by three
# Essence of the Wild tests.
mtg-engine/src/replacement.rs: delete field copy_of from struct EnteringPermanent expression in run_entering_pass

# X-cost funding: three survivors out of 109 flips in build_options/apply/
# validate, each unreachable rather than untested.
#
# `mana_per_tap == 0 || amount == 0`: a group with no output is filtered out
# before it can be built, and the `amount == 0` half makes the loop body a
# no-op either way (`0 / per_tap` taps nothing), so `&&` and `||` do the same
# thing.
mtg-engine/src/funding.rs: replace || with && in apply
# `if after_amount > before_amount` measures what a tap ADDED. Activating a
# mana ability only ever adds, so `>=` differs only for the types it did not
# touch, and those contribute `after - before == 0`.
mtg-engine/src/funding.rs: replace > with >= in apply
# `validate`'s zero-output branch, whose own comment says it cannot happen —
# `build_options` filters such a group out. Defensive against an invariant
# break, so it is kept rather than deleted; `apply` would divide by zero.
mtg-engine/src/funding.rs: replace > with == in validate

# `other_hand_costs`, which excludes the spell being cast from the demand map
# the autotap planner reads. Getting it wrong changes WHICH lands are tapped
# and nothing else — `compute_autotap`'s contract (a plan it offers pays the
# cost) is property-tested and holds either way. Issue #114's exclusion bug
# was real, and its symptom is pinned in mana.rs where the planner lives; the
# preference itself is a knob, not a rule.
mtg-engine/src/engine/legal/casting.rs: replace != with == in from_hand

# A control effect over an object that is not on the battlefield: `move_object`
# drops the effect as the permanent leaves, so the state the flipped
# comparison would flag cannot arise. Adds a false alarm for an impossible
# board rather than blinding a clause.
mtg-engine/src/invariants/effects.rs: replace == with != in check_core
# The don't-double-report guard beside it: a control effect whose controller
# or original controller is out of range is already reported by check_core,
# and this `continue` keeps check_settled from piling a second message on the
# same corruption. Flipping it duplicates a report; it blinds nothing.
mtg-engine/src/invariants/effects.rs: replace || with && in check_settled

# `Vec::with_capacity` for the tokens a doubling effect makes — a capacity
# hint, not a count. Deleted rather than accepted; recorded here so the line
# is not re-filed if an older mutant list surfaces.
#
# `from == Some(Graveyard) && to != Graveyard`, the clause that takes a card
# OUT of the graveyard pile. Equivalent given the clause beside it: the pile
# only ever lists cards in a graveyard, so widening this one to "or the
# destination is not a graveyard" only re-runs a `retain` that removes
# nothing. The clause that maintains that invariant — the one that appends —
# is not accepted and is killed by
# `the_pile_records_arrivals_in_the_graveyard_and_nothing_else`.
mtg-engine/src/state.rs: replace && with || in GameState::move_object

# `resolving_spell` cleared when the tracked spell leaves the stack. Its one
# consumer re-checks the zone before acting, so a stale tracker changes
# nothing there — and the invariant checker asserts the tracker directly
# ("resolving_spell #N is in Graveyard"), so a stale one is reported by the
# nightly fuzzer rather than passing silently. Covered, just not by a unit
# test.
mtg-engine/src/state.rs: replace != with == in GameState::move_object

# ── The 2026-09-07 backlog sweep: what was left after the tests ──────────
#
# 226 mutants over the twelve engine files that still carried backlog lines.
# Most were killed; these are the ones that stayed, each with the reason.

# The auto-pick that pays "exile three creature cards" when the client named
# none. Unreachable: an unnamed cost raises a ChooseExileFromGraveyard prompt,
# so by the time payment runs the set has been named. The payability check
# that decides whether the prompt is raised at all IS covered
# (`an_unnamed_exile_cost_is_only_payable_with_enough_of_the_right_cards`).
mtg-engine/src/engine/costs.rs: replace && with || in pay_exile_creatures

# `reduce`'s leftover reduction, which only escapes as `x_discount` for a
# spell whose cost has BOTH {X} and a generic symbol (CR 601.2f). No card in
# the pool has one, so the subtraction that produces it is unobservable; the
# reduction that does reach a printed cost is pinned by the Manor Gargoyle
# rows in `a_cost_modifier_reaches_the_spells_its_text_names`.
mtg-engine/src/engine/costs.rs: replace -= with += in reduce
mtg-engine/src/engine/costs.rs: replace -= with /= in reduce

# The exile-set answer's count check. Redundant with `additional_cost_is_payable`,
# which requires `exile_ids.len() == n` before anything is paid — the wrong
# count is refused either way, and the second refusal is the one a test sees.
mtg-engine/src/engine/actions/choices.rs: replace || with && in resolve_choice
mtg-engine/src/engine/actions/choices.rs: replace > with < in resolve_choice

# The pile label in the log line ("Pile 1" / "Pile 2"). Which pile was chosen
# is a rules decision and is caught; what it is called in the log is not.
mtg-engine/src/engine/actions/choices.rs: replace == with != in resolve_choice

# `mulligan_keep`'s advance past the player who just kept. Redundant:
# `advance_mulligan_phase` skips players already marked kept, so it steps over
# the same player on its own. Kept because that function's contract says the
# caller advances, and the skip is a safety net rather than the mechanism.
# `mulligan_mull`'s copy of the same line is NOT redundant and is caught.
mtg-engine/src/engine/actions/mulligan.rs: replace += with *= in mulligan_keep

# The debug line noting that a duplicate target was dropped. The drop itself
# is a rules action (CR 601.2c) and is pinned by
# `a_duplicate_in_a_two_target_spells_second_slot_is_dropped`; the Debug-level
# note about it is not.
mtg-engine/src/engine/actions/cast.rs: replace != with == in cast_spell

# The autotap source classifier's "has a utility ability" test, which decides
# which of several correct tap plans is preferred. A preference, like the rest
# of the planner (docs/mutation-testing-guide.md), whose contract is
# property-tested in mana.rs.
mtg-engine/src/engine/mana_sources.rs: delete ! in gather_mana_sources
# And the pool rendering inside the "tapped for mana" log line.
mtg-engine/src/engine/mana_sources.rs: replace > with == in activate_mana_source_reserving

# The attached-permanent walk, reached only for an object with no native
# ability of the index being activated and no copy grantor — which
# `legal_actions` never offers, so it is the submit path guarding against a
# hand-built activation that names an index nothing on that object has.
mtg-engine/src/engine/actions/abilities.rs: replace && with || in activate_ability

# Loyalty payment, `change > 0` / `change < 0`. Widening either to include
# zero moves a 0-loyalty ability (Garruk Relentless's) from "neither branch"
# to a branch that adds or removes zero counters. Equivalent.
mtg-engine/src/engine/actions/abilities.rs: replace > with >= in activate_loyalty_ability
mtg-engine/src/engine/actions/abilities.rs: replace < with <= in activate_loyalty_ability

# The chain that asks for the second of two discarded cards. Its three
# conditions are each unreachable in the pool as it stands: no card raises a
# multi-card hand choice that is not `discard_immediately`; `remaining > 1`
# and `remaining >= 1` differ only on the last ask, where the chain asks for
# zero more and returns at once; and the `awaiting_action.is_none()` guard is
# for a discard whose own hook asked something mid-chain, which nothing does.
# The chain itself is pinned by `a_two_card_discard_asks_twice`.
mtg-engine/src/engine/actions/choices.rs: replace && with || in resolve_choice
mtg-engine/src/engine/actions/choices.rs: replace > with >= in resolve_choice

# `CastMethod::Alternative(c) if alternative_cost.is_some()` — the guard that
# keeps a caller-supplied alternative cost from being reduced twice, while a
# flashback cost still goes through `cost_to_cast`. The two paths differ only
# for a card with flashback AND a cost reduction that reaches it; Heartless
# Summoning is the pool's only reducer and it reads creature spells, of which
# none has flashback.
mtg-engine/src/engine/actions/cast.rs: replace match guard alternative_cost.is_some() with false in cast_spell
mtg-engine/src/engine/actions/cast.rs: replace match guard alternative_cost.is_some() with true in cast_spell

# Clearing `last_activated_x_value` when a non-X ability is activated, so a
# stale X cannot leak into the next reader. Kessig Wolf Run is the only card
# that reads it and it is itself an X ability, so the leak has no audience —
# and the action contract in invariants/transition.rs asserts the field
# directly, so a stale one is reported by the nightly fuzzer rather than
# passing silently.
mtg-engine/src/engine/actions/abilities.rs: delete ! in activate_ability

# ── Retired tests, 2026-09-07 ────────────────────────────────────────────
#
# Six mutation-motivated tests were deleted for failing the standard in
# docs/mutation-testing-guide.md — they pinned a heuristic, a presentation
# order, a display format or a getter, none of which an engine user could see
# go wrong. The mutants they had been killing come back here, with the reason
# each is arid rather than untested.

# The autotap planner's ranking inputs: how many colours a source can make,
# and how much the rest of the hand wants them. Nothing about a plan is right
# or wrong because of these — they choose among plans that are all correct,
# and the contract they serve (an offered plan pays the cost; a declined one
# had no plan) is property-tested in mana.rs. The deleted tests asserted the
# scores and the resulting sort order directly, which is the guide's worked
# example of what not to write.
mtg-engine/src/mana.rs: replace source_flexibility -> usize with 0
mtg-engine/src/mana.rs: replace source_flexibility -> usize with 1
mtg-engine/src/mana.rs: replace source_sort_key -> (ManaSourceKind, usize, u32) with (Default::default(), 0, 0)
mtg-engine/src/mana.rs: replace source_sort_key -> (ManaSourceKind, usize, u32) with (Default::default(), 0, 1)
mtg-engine/src/mana.rs: replace source_sort_key -> (ManaSourceKind, usize, u32) with (Default::default(), 1, 0)
mtg-engine/src/mana.rs: replace source_sort_key -> (ManaSourceKind, usize, u32) with (Default::default(), 1, 1)
mtg-engine/src/mana.rs: replace += with *= in build_hand_demand
mtg-engine/src/mana.rs: replace += with -= in build_hand_demand
mtg-engine/src/mana.rs: replace += with *= in hand_demand_score
mtg-engine/src/mana.rs: replace += with -= in hand_demand_score

# The funding prompt's group ordering. A response names groups by NAME, so
# position carries nothing, and any order the comparator produces is
# deterministic — which is the property the replay check depends on. Masks the
# other `==` sites in the same function, which ARE killed, by
# `a_summoning_sick_dork_cannot_fund_x_but_a_ready_one_can`.
mtg-engine/src/funding.rs: replace == with != in build_options

# How a tap plan is rendered into a prompt's description — the grouping of
# runs ("tap 2x Swamp, Sol Ring") and the counting behind it. WHICH sources a
# plan taps is the rules question and is pinned where the planner lives; how
# the list is punctuated is not, and any correct rendering says the same
# thing.
mtg-engine/src/cards/helpers.rs: replace += with *= in format_tap_plan_names
mtg-engine/src/cards/helpers.rs: replace += with -= in format_tap_plan_names
mtg-engine/src/cards/helpers.rs: replace == with != in format_tap_plan_names

# Mutation-testing backlog: genuine test gaps, tracked but not yet closed.
#
# Every line is a surviving mutant (line:col stripped, same normalization
# as reports/mutants-accepted.txt) that a test SHOULD kill someday — none
# of these are judged equivalent. They live here so the weekly-mutants
# workflow doesn't re-file the same known survivors every Saturday; the
# consolidated tracking issue and the daily bug fixer work this list down.
# When a test kills one, delete its line. When one is judged equivalent
# after all, move it to reports/mutants-accepted.txt with a reason.
#
# A line whose mutant no longer exists (the code it described was
# rewritten) is deleted too: awaiting.rs's bitmask subset enumeration
# became `targeting::combinations`, taking four entries with it.
#
# Being on this list is not a promise that a test will be written. It means
# nobody has triaged it yet. docs/mutation-testing-guide.md is the decision
# procedure: some of these will turn out to be heuristics, unreachable code,
# or bookkeeping inside a computation whose result is already property-tested,
# and those move to reports/mutants-accepted.txt with a reason rather than
# acquiring a test that would freeze the implementation.
#
# Rebuilt 2026-09-07 from a 226-mutant scoped sweep over the twelve engine
# files this list named. 158 caught, 67 missed; the missed were triaged under
# docs/mutation-testing-guide.md into tests, deletions, and accepted-with-a-
# reason. What is left below is what that sweep still reports and nobody has
# judged yet.
mtg-engine/src/engine.rs: replace && with || in run_game_loop_inner
mtg-engine/src/engine.rs: replace == with != in run_game_loop_inner
#
# What these two are: the "fresh first turn" detection at the top of
# `run_game_loop_inner` — `is_first_turn && turn_number == 1 && step == Untap
# && priority_player.is_none() && awaiting_action.is_none()` — which decides
# whether to emit turn 1's `TurnStarted` and `StepStarted`. Loosening it makes
# a RESUMED game emit them a second time, and with them a second upkeep. The
# test wants a game resumed from a save at turn 1 untap driven through the
# real loop, which nothing does yet.


# From the 2026-09-08 re-run of issues #276–#279: what survived both the
# self-test battery and the tests written in that pass. They are the same two
# shapes throughout — a clause whose conjuncts want one state each, and a
# clause whose bad state needs a card the pool does not have — and the triage
# is written up in reports/mutation-testing.md.
mtg-engine/src/invariants/events.rs: delete match arm GameEvent::CardDrawn{player, ..} in steps
mtg-engine/src/invariants/events.rs: delete match arm GameEvent::LeftBattlefield{object, ..} in blockers
mtg-engine/src/invariants/events.rs: replace && with || in damage
mtg-engine/src/invariants/events.rs: replace && with || in spells_and_lands
mtg-engine/src/invariants/events.rs: replace && with || in zone_changes
mtg-engine/src/invariants/events.rs: replace == with != in names_object
mtg-engine/src/invariants/events.rs: replace > with >= in steps
mtg-engine/src/invariants/events.rs: replace match guard *amount == 0 with true in steps
mtg-engine/src/invariants/events.rs: replace || with && in damage
mtg-engine/src/invariants/events.rs: replace || with && in steps
mtg-engine/src/invariants/legal.rs: delete ! in combat_prompt
mtg-engine/src/invariants/legal.rs: delete ! in tap_plan_ok
mtg-engine/src/invariants/legal.rs: delete match arm Action::ActivateAbility{object_id, targets, sacrifice, tap_plan, ..} in prompt_offers
mtg-engine/src/invariants/legal.rs: delete match arm Action::CastSpell{object_id, targets, sacrifice, exile_ids, tap_plan, ..} in prompt_offers
mtg-engine/src/invariants/legal.rs: delete match arm Action::DiscardCards{cards} | Action::BottomCards{cards} in prompt_offers
mtg-engine/src/invariants/legal.rs: delete match arm Action::PlayLand{object_id} | Action::ActivateManaAbility{object_id, ..} in prompt_offers
mtg-engine/src/invariants/legal.rs: replace != with == in prompt_offers
mtg-engine/src/invariants/legal.rs: replace && with || in activate
mtg-engine/src/invariants/legal.rs: replace && with || in check_legal
mtg-engine/src/invariants/legal.rs: replace && with || in play_land
mtg-engine/src/invariants/legal.rs: replace && with || in tap_plan_ok
mtg-engine/src/invariants/legal.rs: replace - with + in choose
mtg-engine/src/invariants/legal.rs: replace < with <= in combat_prompt
mtg-engine/src/invariants/legal.rs: replace < with <= in loyalty
mtg-engine/src/invariants/legal.rs: replace == with != in activate
mtg-engine/src/invariants/legal.rs: replace == with != in cast
mtg-engine/src/invariants/legal.rs: replace || with && in activate
mtg-engine/src/invariants/legal.rs: replace || with && in cast
mtg-engine/src/invariants/legal.rs: replace || with && in check_legal
mtg-engine/src/invariants/prompts.rs: delete match arm K::PayOrNot{source_spell_id, ..} in check_choice
mtg-engine/src/invariants/prompts.rs: delete match arm K::YesNo{source_card, ..} in check_choice
mtg-engine/src/invariants/prompts.rs: delete match arm PendingEffect::CantBlockThisTurn{..} in check_choice
mtg-engine/src/invariants/prompts.rs: delete match arm PendingEffect::DebuffUntilEOT{..} in check_choice
mtg-engine/src/invariants/prompts.rs: replace && with || in check_choice
mtg-engine/src/invariants/prompts.rs: replace && with || in library_option
mtg-engine/src/invariants/prompts.rs: replace < with <= in check_choice
mtg-engine/src/invariants/prompts.rs: replace match guard !player_ok(state, *p) with true in check_choice
mtg-engine/src/invariants/prompts.rs: replace || with && in tba_common
mtg-engine/src/invariants/stack.rs: delete ! in check_core
mtg-engine/src/invariants/stack.rs: delete match arm TriggerEvent::StateTriggered in check_core
mtg-engine/src/invariants/stack.rs: replace && with || in check_core
mtg-engine/src/invariants/stack.rs: replace == with != in check_core
mtg-engine/src/invariants/stack.rs: replace > with >= in check_core
mtg-engine/src/invariants/stack.rs: replace match guard *target == obj.id with false in check_core
mtg-engine/src/invariants/stack.rs: replace match guard *target == obj.id with true in check_core
mtg-engine/src/invariants/stack.rs: replace match guard o.zone != Zone::Stack with true in check_core
mtg-engine/src/invariants/transition.rs: delete ! in life_and_loss
mtg-engine/src/invariants/transition.rs: delete ! in monotone
mtg-engine/src/invariants/transition.rs: delete ! in per_turn
mtg-engine/src/invariants/transition.rs: delete ! in status_ledgers
mtg-engine/src/invariants/transition.rs: delete ! in walk
mtg-engine/src/invariants/transition.rs: delete match arm (Some(a), Some(b)) in status_ledgers
mtg-engine/src/invariants/transition.rs: delete match arm GameEvent::Untapped{object} in status_ledgers
mtg-engine/src/invariants/transition.rs: replace != with == in action_contract
mtg-engine/src/invariants/transition.rs: replace != with == in per_turn
mtg-engine/src/invariants/transition.rs: replace != with == in status_ledgers
mtg-engine/src/invariants/transition.rs: replace != with == in walk
mtg-engine/src/invariants/transition.rs: replace % with / in walk
mtg-engine/src/invariants/transition.rs: replace && with || in action_contract
mtg-engine/src/invariants/transition.rs: replace && with || in identity
mtg-engine/src/invariants/transition.rs: replace && with || in life_and_loss
mtg-engine/src/invariants/transition.rs: replace && with || in mana_ledger
mtg-engine/src/invariants/transition.rs: replace && with || in monotone
mtg-engine/src/invariants/transition.rs: replace && with || in per_turn
mtg-engine/src/invariants/transition.rs: replace && with || in status_ledgers
mtg-engine/src/invariants/transition.rs: replace && with || in walk
mtg-engine/src/invariants/transition.rs: replace && with || in zone_ledger
mtg-engine/src/invariants/transition.rs: replace + with * in action_contract
mtg-engine/src/invariants/transition.rs: replace + with * in per_turn
mtg-engine/src/invariants/transition.rs: replace + with * in status_ledgers
mtg-engine/src/invariants/transition.rs: replace + with * in walk
mtg-engine/src/invariants/transition.rs: replace + with - in action_contract
mtg-engine/src/invariants/transition.rs: replace + with - in mana_demanded
mtg-engine/src/invariants/transition.rs: replace + with - in per_turn
mtg-engine/src/invariants/transition.rs: replace + with - in status_ledgers
mtg-engine/src/invariants/transition.rs: replace + with - in walk
mtg-engine/src/invariants/transition.rs: replace += with *= in action_contract
mtg-engine/src/invariants/transition.rs: replace - with + in action_contract
mtg-engine/src/invariants/transition.rs: replace - with / in action_contract
mtg-engine/src/invariants/transition.rs: replace < with <= in monotone
mtg-engine/src/invariants/transition.rs: replace <= with > in life_and_loss
mtg-engine/src/invariants/transition.rs: replace == with != in action_contract
mtg-engine/src/invariants/transition.rs: replace == with != in identity
mtg-engine/src/invariants/transition.rs: replace == with != in life_and_loss
mtg-engine/src/invariants/transition.rs: replace == with != in mana_added
mtg-engine/src/invariants/transition.rs: replace == with != in per_turn
mtg-engine/src/invariants/transition.rs: replace == with != in status_ledgers
mtg-engine/src/invariants/transition.rs: replace == with != in walk
mtg-engine/src/invariants/transition.rs: replace == with != in zone_ledger
mtg-engine/src/invariants/transition.rs: replace > with < in action_contract
mtg-engine/src/invariants/transition.rs: replace > with < in identity
mtg-engine/src/invariants/transition.rs: replace > with < in life_and_loss
mtg-engine/src/invariants/transition.rs: replace > with < in walk
mtg-engine/src/invariants/transition.rs: replace > with == in action_contract
mtg-engine/src/invariants/transition.rs: replace > with == in identity
mtg-engine/src/invariants/transition.rs: replace > with == in life_and_loss
mtg-engine/src/invariants/transition.rs: replace > with == in walk
mtg-engine/src/invariants/transition.rs: replace > with >= in action_contract
mtg-engine/src/invariants/transition.rs: replace > with >= in identity
mtg-engine/src/invariants/transition.rs: replace > with >= in life_and_loss
mtg-engine/src/invariants/transition.rs: replace > with >= in walk
mtg-engine/src/invariants/transition.rs: replace match guard *o == b.id with false in status_ledgers
mtg-engine/src/invariants/transition.rs: replace match guard *o == b.id with true in status_ledgers
mtg-engine/src/invariants/transition.rs: replace match guard *player == p && *mana_type == t with true in mana_ledger
mtg-engine/src/invariants/transition.rs: replace match guard *player == p with true in life_and_loss
mtg-engine/src/invariants/transition.rs: replace match guard *player == who && *mana_type == t with false in action_contract
mtg-engine/src/invariants/transition.rs: replace match guard *player == who && *mana_type == t with true in action_contract
mtg-engine/src/invariants/transition.rs: replace match guard *player == who with true in action_contract
mtg-engine/src/invariants/transition.rs: replace match guard c2.object_id != id with false in action_contract
mtg-engine/src/invariants/transition.rs: replace match guard c2.object_id != id with true in action_contract
mtg-engine/src/invariants/transition.rs: replace match guard cur.get_object(*object).is_some_and(|o| o.owner == p) with false in status_ledgers
mtg-engine/src/invariants/transition.rs: replace match guard cur.get_object(*object).is_some_and(|o| o.owner == p) with true in status_ledgers
mtg-engine/src/invariants/transition.rs: replace || with && in action_contract
mtg-engine/src/invariants/transition.rs: replace || with && in identity
mtg-engine/src/invariants/transition.rs: replace || with && in life_and_loss
mtg-engine/src/invariants/transition.rs: replace || with && in status_ledgers
mtg-engine/src/invariants/transition.rs: replace || with && in walk

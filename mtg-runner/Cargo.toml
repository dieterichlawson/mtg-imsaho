[package]
name = "mtg-runner"
version = "0.1.0"
edition = "2021"

[dependencies]
mtg-engine = { path = "../mtg-engine" }
mtg-player = { path = "../mtg-player" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true

[dev-dependencies]
libc = "0.2"
